"""Disposition: steer an external AI coding tool to write in your style.

This is the M0 skeleton. It wires up the package, config, the store with the
two-key Cascade merge, a CLI shell, and a hello-world MCP server. The real
capture, induction, retrieval, and verification stages arrive in later
milestones (see docs/v1-implementation-plan.md).
"""

__version__ = "0.3.0"
